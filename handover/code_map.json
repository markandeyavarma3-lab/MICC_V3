{
  "_meta": {
    "generated_from": "AST scan (docstrings + top-level def/class signatures) + grep of internal `from src...` imports, at commit 45fe5b0",
    "note": "Every path below exists in this repo's src/ tree at the time of generation. `deps` lists internal (src.*) imports only, extracted by grep of `^from src|^import src` — stdlib/third-party deps are omitted. `status` is copied from docs/STATUS.md (derived at commit 472dfa8, 5 commits behind HEAD) where a corresponding step exists; 'unverified' means no direct STATUS.md step maps to this file."
  },
  "src/__init__.py": { "purpose": "package marker, empty", "deps": [] },

  "src/archive/stopgap.py": { "purpose": "capture today's raw bulk/block deal bytes before they roll off the rolling current-day endpoint; nothing else", "deps": ["src.common.hashing", "src.common.paths"], "status": "VERIFIED (2.1-2.3)", "public": ["Source", "capture(src)", "main()"] },
  "src/archive/prices.py": { "purpose": "daily NSE bhavcopy price feed archiver", "deps": ["src.common.hashing", "src.common.paths"], "status": "VERIFIED (2.12)", "public": ["capture(session, today)", "collect(start, end, max_per_run)", "main()"] },
  "src/archive/insider.py": { "purpose": "SEBI insider-trading XBRL filings archiver; empty-envelope-is-a-failure rule closes a 2-month silent loss", "deps": ["src.common.hashing", "src.common.paths"], "status": "unverified", "public": ["capture_window(op, frm, to, seen, budget)", "collect(start, end, max_detail)", "main()"] },
  "src/archive/corporate_actions.py": { "purpose": "corporate actions archiver, the splice guard could not see them without it", "deps": ["src.common.hashing", "src.common.paths"], "status": "VERIFIED (2.14)", "public": ["capture(op, frm, to)", "collect(start, end)", "main()"] },
  "src/archive/derivatives.py": { "purpose": "F&O participant-wise open interest + F&O bhavcopy archiver, decision 0058 — collection only, deliberately not wired to any parser/study", "deps": ["src.common.hashing", "src.common.paths"], "status": "new 2026-09-11, unverified in STATUS.md snapshot used (post-dates it)", "public": ["Feed", "capture(f, session, today)", "main()"] },

  "src/common/paths.py": { "purpose": "every filesystem/database location, resolved from repo root; RESEARCH_ENV-scoped, raises rather than defaults", "deps": [], "status": "VERIFIED (1.3, partial — no structured logging/central config loader)", "public": ["env()", "data_dir(e)", "warehouse_dir(e)", "snapshot_dir(e)", "research_db(e)", "governance_db(e)", "review_db(e)", "ensure_dirs(e)"], "known_issue": "snapshot_dir()/review_db() historically defined but never read anywhere — check if still true" },
  "src/common/calendar.py": { "purpose": "OBSERVED trading calendar, never generated — 5,358 sessions incl. 3 Saturdays", "deps": ["src.common.paths"], "status": "VERIFIED (1.4)", "public": ["sessions(env)", "is_session(d, env)", "next_session(d, env)", "session_offset(d, n, env)"], "known_issue": "lru_cache keyed under env=None historically caused stale calendar on RESEARCH_ENV change mid-process" },
  "src/common/hashing.py": { "purpose": "content addressing for the provenance DAG", "deps": [], "status": "VERIFIED (1.3)", "public": ["hash_bytes", "hash_file", "hash_params", "hash_inputs", "spec_hash", "merkle_root"] },
  "src/common/migrate.py": { "purpose": "forward-only, checksummed schema migrations for both DuckDB and SQLite", "deps": ["src.common.hashing", "src.common.paths"], "status": "VERIFIED (1.5)", "public": ["migrate_sqlite(db_path, directory, dry_run)", "migrate_duckdb(db_path, directory, dry_run)", "status(db_path, directory, target)"], "known_issue": "migrate_sqlite historically used con.executescript() which implicitly commits before running, so the except/rollback cannot undo a partially-applied migration; the DuckDB path uses explicit BEGIN/COMMIT correctly" },

  "src/governance/ledger.py": { "purpose": "write-guards added 2026-09-12: refuse writes to a missing/schemaless/empty governance ledger, refuse a non-identical re-registration", "deps": ["src.common.paths"], "status": "new, VERIFIED via 12 tests in tests/test_ledger_guards.py (post-dates 472dfa8 STATUS.md snapshot)", "public": ["require_populated_ledger(env)", "require_not_already_registered(experiment_id, spec_hash, env)", "LedgerRefused"] },
  "src/governance/provenance.py": { "purpose": "content-addressed provenance DAG — artefact/artefact_edge/merkle_log", "deps": ["src.common.hashing", "src.common.migrate", "src.common.paths"], "status": "VERIFIED (1.9)", "public": ["register(artefact, parents, env)", "register_file(...)", "register_dataset(...)", "register_config(path, env)", "lineage(artefact_hash, env)", "descendants(artefact_hash, env)", "write_merkle_root(as_of, env)", "data_checksum(con, glob, columns)"] },

  "src/identity/master.py": { "purpose": "point-in-time symbol -> security resolution; builds security_master + symbol_history from the two-date-format ISIN master", "deps": ["src.common.migrate", "src.common.paths", "src.governance.provenance"], "status": "VERIFIED (3.1, 3.2)", "public": ["build(env)", "resolve_all(env)", "RESOLVE_SQL", "main()"], "known_issue": "resolved security_id historically not consumed by measure.py/consensus.py/selling.py for the price join — see handover/04_WHAT_IS_MISSING.md item B5" },

  "src/ingest/parse.py": { "purpose": "turn archived bytes into rows without ever losing the bytes", "deps": ["src.common.hashing", "src.common.paths"], "status": "VERIFIED (2.1, per current STATUS.md — historically crashed on real PRICE/INSIDER files, audit BUG-1, re-verify)", "public": ["parse_csv(body, report_type)", "parse_fii_dii(body)", "parse_archive(path)", "parse_all(root)", "main()"] },
  "src/ingest/land.py": { "purpose": "the ONLY path collected/parsed deals enter research_prod.duckdb", "deps": ["src.common.migrate", "src.common.paths", "src.governance.provenance", "src.ingest.parse"], "status": "VERIFIED (2.1)", "public": ["land(env)", "land_file(con, pf, report)", "main()"] },
  "src/ingest/bhavcopy.py": { "purpose": "archived NSE bhavcopy -> price-spine increment parquet", "deps": ["src.common.paths", "src.governance.provenance"], "status": "VERIFIED (2.12)", "public": ["run(overwrite)", "register(env)", "main()"] },
  "src/ingest/corp_actions.py": { "purpose": "archived NSE corporate actions -> typed, factored table", "deps": ["src.common.paths", "src.governance.provenance"], "status": "VERIFIED (2.14)", "public": ["classify(subject)", "parse()", "write(actions)", "main()"] },
  "src/ingest/insider.py": { "purpose": "archived insider XBRL -> typed transactions", "deps": ["src.common.paths", "src.governance.provenance"], "status": "unverified", "public": ["parse_file(path)", "parse()", "write(txns)", "main()"] },
  "src/ingest/publication.py": { "purpose": "measures available_from empirically per source, never assumes it", "deps": ["src.common.paths"], "status": "VERIFIED (2.10, note: brackets 10.7h at best; nothing downstream consumes the measurement per last audit)", "public": ["bounds(path)", "summary(bs)", "main()"] },
  "src/ingest/seed_deals.py": { "purpose": "lands the twenty-year predecessor deal corpus, honestly labelled", "deps": ["src.common.hashing", "src.common.migrate", "src.common.paths", "src.governance.provenance"], "status": "unverified", "public": ["land_seed(env)", "main()"] },

  "src/mart/clean.py": { "purpose": "institutional_deals_clean — zero silent drops, explicit ineligibility reason on every row", "deps": ["src.common.paths", "src.governance.provenance", "src.identity.master (RESOLVE_SQL)", "src.mart.eligibility"], "status": "VERIFIED (4.1, 4.6)", "public": ["build(env, t)", "participation_ceiling()", "main()"], "known_issue": "5 of 6 declared flag columns historically FALSE/NULL on every row (duplicate_group_id, five_day_round_trip_flag, internal_transfer_flag, promoter_related_flag, suspect_flag)" },
  "src/mart/eligibility.py": { "purpose": "which disclosed deals are research events — a SECOND, partially divergent PROP_HFT classifier implementation vs. clean.py", "deps": ["src.common.paths"], "status": "unverified — historically only Thresholds/spec imported by production code, classify/grid/summarise unused", "public": ["spec()", "Thresholds", "grid()", "classify(con, t)", "main()"] },

  "src/monitor/status.py": { "purpose": "generates docs/STATUS.md by deriving phase completion from repo + db state, never hand-asserted", "deps": ["src.common.paths", "src.warehouse.reconcile"], "status": "VERIFIED (8) per its own grading; historically had self-referential predicates inflating 12 step grades (fixed? re-verify)", "public": ["steps()", "evaluate()", "render(rep)", "write(path)", "main()"] },
  "src/monitor/health.py": { "purpose": "generates docs/HEALTH.md; detects stale/gapped collection sources, desktop+email alerting", "deps": ["src.common.paths", "src.monitor.backup_state"], "status": "VERIFIED (2.13) per current STATUS; historically blind to mid-series gaps (latest-session-only design) — re-verify current read() logic", "public": ["read()", "check(write_file, send)", "notify_desktop(title, message)", "notify_email(subject, body)", "main()"] },
  "src/monitor/inventory.py": { "purpose": "generates docs/DATA_INVENTORY.md — every table on disk, counted rather than remembered", "deps": ["src.common.paths"], "status": "unverified", "public": ["collect(env)", "databases(env)", "render(tables)", "main()"] },
  "src/monitor/backup_state.py": { "purpose": "is there a current off-machine backup, and what is missing from it?", "deps": ["src.common.paths"], "status": "unverified", "public": ["destination()", "read()", "BackupState"] },

  "src/research/power.py": { "purpose": "statistical power computed BEFORE the fit — cohort collapse, block bootstrap, serial-correlation-corrected MDE; the strongest code in the repo", "deps": [], "status": "VERIFIED (6.2)", "public": ["cohort_collapse(event_dates, relative_returns, freq)", "mde(sd, n_periods, power, alpha)", "verdict(observed, detectable, plausible_bound, p_value, alpha)", "block_bootstrap_ci(cohorts, block_length, draws, seed, ci)", "serial_inflation(cohorts, max_lag, label_periods)", "mde_serial_corrected(...)"] },
  "src/research/multiplicity.py": { "purpose": "turns the trial counter into an actual statistical bar via Gumbel-limit best-of-N, Romano-Wolf stepdown", "deps": [], "status": "VERIFIED (6.8)", "public": ["expected_max_null_t(trials, dof)", "sidak_alpha(alpha, trials)", "bar(trials, alpha, margin, mean_correlation, dof)", "romano_wolf(observed, bootstrap, two_sided)"] },
  "src/research/design.py": { "purpose": "what a study must establish (power, confounds, mechanism, economics) before it is allowed to exist — StudyDesign gate", "deps": ["src.common.paths", "src.research.multiplicity"], "status": "historically zero production call sites (no study module constructed a StudyDesign) — re-verify", "public": ["StudyDesign", "HorizonPower", "ConfoundPlan", "required_t()", "powered_horizons()"] },
  "src/research/split.py": { "purpose": "EXPLORE/SELECT/CONFIRM partition — ISIN-keyed sha256 bucket assignment, ConfirmationGuard/ScanGuard", "deps": ["src.common.paths"], "status": "historically zero production call sites for the guards — re-verify", "public": ["split_key(symbol, isin)", "bucket(key)", "assign(symbol, isin)", "ConfirmationGuard", "ScanGuard"] },
  "src/research/families.py": { "purpose": "trial-family charging against family_charge (append-only, monotonic, SQLite-trigger-enforced)", "deps": ["src.common.paths", "src.research.multiplicity"], "status": "family_charge now has 5 rows per 2026-09-11 inventory — some charging is happening; confirm it's the study modules themselves calling this rather than only the register_*.py scripts", "public": ["charge(family_id, trials_added, dof)", "commit_charge(family_id, trials_added, description, experiment_id, dof, env)", "project_bar(dof)"] },
  "src/research/confounds.py": { "purpose": "the standing 9-point confound checklist, run on EXPLORE data, charged to a family", "deps": ["src.common.paths", "src.research.measure", "src.research.split"], "status": "unverified", "public": ["run(env, sessions)"] },
  "src/research/measure.py": { "purpose": "regenerates the power grid decision 0034 rests on — the primary horizon study", "deps": ["src.common.paths", "src.research.power"], "status": "VERIFIED, pins MDE to 13.2701% +/-5e-6 on 4,766 events (per last audit); does NOT import src.identity.master — see handover/04_WHAT_IS_MISSING.md item B5", "public": ["grid(env, cutoff)", "_returns_sql(spine, sessions, cutoff)", "main()"] },
  "src/research/consensus.py": { "purpose": "multi-institution convergence study — the originally-declared critical-path study (decision 0031)", "deps": ["src.common.paths", "src.research.measure", "src.research.power"], "status": "does NOT import src.identity.master — see handover/04_WHAT_IS_MISSING.md item B5", "public": ["grid(env)", "_events_sql(strict)", "main()"] },
  "src/research/selling.py": { "purpose": "the selling study — 34,270 events, the last route to a registrable result explored", "deps": ["src.common.paths", "src.research.measure", "src.research.power"], "status": "historically estimated an effect OUTSIDE the ConfirmationGuard, which decision 0035 forbids in writing — re-verify; does NOT import src.identity.master", "public": ["grid(env)", "main()"] },
  "src/research/entity_verdict.py": { "purpose": "exp_002 — the entity-persistence study, Workstream 3", "deps": ["src.common.paths", "src.research.costs", "src.research.measure", "src.research.entity_names", "src.research.roles"], "status": "unverified", "public": ["build(env, sessions)", "_score(rows)", "_bh(pvals, m)"] },
  "src/research/nullcal.py": { "purpose": "null calibration — permute participant labels within date, re-run the identical procedure", "deps": ["src.common.paths", "src.research.multiplicity"], "status": "VERIFIED (6.9) — 1.8% false-positive rate at nominal 5%", "public": ["run(sessions, env, permutations, seed)", "main()"] },
  "src/research/outcomes.py": { "purpose": "deal_forward_outcomes builder — 9 horizons x 6 declared benchmarks (5 actually buildable)", "deps": ["src.common.paths", "src.research.costs"], "status": "VERIFIED (6.3) — 52,365 outcomes, 237,271 benchmark rows against 5 of 6 benchmarks (NIFTY500_TR unbuildable)", "public": ["build(env, cutoff)", "horizons()", "main()"] },
  "src/research/costs.py": { "purpose": "the transaction-cost model — statutory fees, Corwin-Schultz/Abdi-Ranaldo spread, sqrt-law impact, VIX regime multiplier", "deps": ["src.common.paths"], "status": "VERIFIED (5.1); historically 1 of ~40 costs.yml keys actually read (participation) — re-verify wiring of spread/impact estimators", "public": ["statutory_cost(...)", "round_trip_bps(...)", "corwin_schultz_spread(high, low)", "sqrt_impact(...)", "cost_scenarios(...)"] },
  "src/research/delisting.py": { "purpose": "prices events that stop trading — 3 recovery-factor sensitivities (0.0/0.25/0.50)", "deps": ["src.common.paths", "src.research.confounds", "src.research.measure"], "status": "VERIFIED (6.4) — measured on EXPLORE sells, NOT persisted into deal_forward_outcomes (0 rows there for this dimension); MERGED/SUSPENDED cannot be separated since delisting_reason is UNKNOWN on every row", "public": ["run(env, sessions)"] },
  "src/research/charmatch.py": { "purpose": "the CHAR_MATCHED characteristic-matched benchmark — size/momentum/volatility/industry DGTW-style buckets", "deps": ["src.common.paths"], "status": "WIRED (5.6) per current STATUS.md, runs per event in outcomes.py — historically had 251 lines nothing imported, re-verify this is now a real consumer relationship not just co-located code", "public": ["build_panel(env, buckets)", "main()"] },
  "src/research/insider_power.py": { "purpose": "power analysis (only) on insider-filing populations decision 0046 measured ad hoc", "deps": ["src.common.paths", "src.research.measure", "src.research.power"], "status": "unverified", "public": ["grid(env)", "main()"] },
  "src/research/seasonality_power.py": { "purpose": "Engine 2 (seasonality) feasibility arithmetic — no data query, pure power math; VERDICT DEAD, pinned by 24 data-free tests", "deps": ["src.research.power"], "status": "new 2026-09-12, killed the track before any seasonality_cell was ever computed — see docs/reports/SEASONALITY_POWER.md", "public": ["mde_monthly_bps(...)", "years_required(...)", "n_eff(n_names, rho)"] },
  "src/research/roles.py": { "purpose": "classifies what economic role a counterparty is playing", "deps": [], "status": "unverified", "public": ["classify(name)"] },
  "src/research/entity_names.py": { "purpose": "counting-grade normalisation of deal counterparty strings", "deps": [], "status": "unverified", "public": ["normalize(raw)"] },

  "src/warehouse/seed.py": { "purpose": "carries the irreplaceable 2.5GB predecessor history into this repo, hash-verified", "deps": ["src.common.hashing", "src.common.paths", "src.governance.provenance"], "status": "VERIFIED (1.7)", "public": ["carry(dry_run, env)", "verify(env)", "main()"] },
  "src/warehouse/spine.py": { "purpose": "rebuilds price_spine, price_spine_adj, fno_spine from the carried seed + collected increments", "deps": ["src.common.paths", "src.governance.provenance"], "status": "VERIFIED (1.8) — adjusted spine reaches 2026-09-10 per current STATUS.md; historically 9 unexplained >35% discontinuities survive vs a budget of 12, re-verify", "public": ["build(spec, env, con)", "build_adjusted(env, con)", "build_all(env)", "main()"], "known_issue": "SpineSpec.unique_key historically declared, unit-tested for existence, never used in a query" },
  "src/warehouse/reconcile.py": { "purpose": "the Phase-1 gate, tolerance 0 against MICCV2-scoped reconciliation targets in configs/universe.yml", "deps": ["src.common.paths", "src.governance.provenance"], "status": "VERIFIED — 20/20 per last full run recorded", "public": ["run(env)", "quality(env)", "main()"] },
  "src/warehouse/benchmarks.py": { "purpose": "builds the daily 6-benchmark panel", "deps": ["src.common.paths"], "status": "WIRED (5.6, partial — 5 of 6 buildable)", "public": ["build(env)", "declared_ids()", "main()"] },
  "src/warehouse/participant_oi.py": { "purpose": "loads the F&O participant-wise OI proxy from the seed into the participant_oi table (migration 0004)", "deps": ["src.common.hashing", "src.common.migrate", "src.common.paths", "src.governance.provenance"], "status": "VERIFIED (2.8) — 15,359 rows loaded", "public": ["load(env)", "main()"] }
}
